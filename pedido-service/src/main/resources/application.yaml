spring:
  application:
    name: pedido-service
  datasource:
    url: jdbc:postgresql://localhost:5432/microsservicos_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8083

clientes:
  base-url: http://localhost:8081

produtos:
  base-url: http://localhost:8082